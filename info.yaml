all:
  children:

    ubuntu:
      vars:
       ansible_user: jp
      hosts:
        server1:
          ansible_host: 192.168.56.101
          ansible_user: jppios
          os_type: Ubuntu

    linuxservers:
       children:
         ubuntu:

    servers:
      hosts:
        server1:

